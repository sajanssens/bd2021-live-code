Author marcel=new Author();

Author bea=marcel; // = uitspreken als "wordt"

int i=9;

if(i==10){ // == uitspreken als "is (gelijk aan)"

}

// Methode declareren:

.A..B..C.(.D.) {
    // return ...; // is van type B
    // geen return = void
}

/*
    A: public/protected/- ("package private")/private // van meeste naar minste zichtbaarheid
    B: return type van deze methode
    C: kies een leuke naam (camel case, zonder spaties, is een identifier, regels (bijv. niet met getal beginnen)
    D: OPTIONEEL! een komma gescheiden lijst van parameters; één param is: <type> <identifier> (i.e. variabele), bijv.: String naam, int age
*/
